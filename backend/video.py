import sys
import base64
import requests

data=sys.argv[1]
# binary_data = base64.b64decode(data)
print(data)
# binary_data=bytes.fromhex(data)
# with open('video.mp4', 'wb') as f:
    # f.write(binary_data)